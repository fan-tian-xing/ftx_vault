# 深度学习考试题（三套）参考答案

---

# 第一套参考答案

## 一、填空题

1. $H(p)=-\sum_{i=1}^K p_i\log_2 p_i$；比特。

2. $0$；$P=Q$，即两个分布几乎处处相同。

3. $\sigma'(z)=\sigma(z)(1-\sigma(z))$。

4. $\frac{\lambda}{2}\|w\|_2^2$。

5. $W_2=\left\lfloor\frac{W_1-F+2P}{S}\right\rfloor+1$。

## 二、计算题

1. 对多元高斯分布，
   $$
   D_{KL}(P\|Q)=\frac{1}{2}\left[\operatorname{tr}(\Sigma_2^{-1}\Sigma_1)+(\mu_2-\mu_1)^T\Sigma_2^{-1}(\mu_2-\mu_1)-n+\ln\frac{\det\Sigma_2}{\det\Sigma_1}\right].
   $$
   本题中 $n=2,\Sigma_1=I,\Sigma_2=4I$，因此
   $$
   \operatorname{tr}(\Sigma_2^{-1}\Sigma_1)=\operatorname{tr}\left(\frac{1}{4}I\right)=\frac{1}{2},
   $$
   $$
   \ln\frac{\det\Sigma_2}{\det\Sigma_1}=\ln 16.
   $$
   所以
   $$
   D_{KL}(P\|Q)=\frac{1}{2}\left(\frac{1}{2}-2+\ln16\right)
   =\ln4-\frac{3}{4}\approx 0.6363.
   $$

2. Softmax 分母为
   $$
   e^1+e^0+e^{-1}=e+1+e^{-1}.
   $$
   因此
   $$
   a=\left(\frac{e}{e+1+e^{-1}},\frac{1}{e+1+e^{-1}},\frac{e^{-1}}{e+1+e^{-1}}\right)^T
   \approx (0.6652,0.2447,0.0900)^T.
   $$
   损失为
   $$
   L=-\ln a_1\approx 0.4076.
   $$
   对 logits 的梯度为
   $$
   \nabla_z L=a-y\approx (-0.3348,0.2447,0.0900)^T.
   $$

3. 先计算预测值：
   $$
   Vx=
   \begin{pmatrix}
   1 & -1\\
   0 & 2
   \end{pmatrix}
   \begin{pmatrix}
   2\\
   1
   \end{pmatrix}
   =
   \begin{pmatrix}
   1\\
   2
   \end{pmatrix}.
   $$
   误差为
   $$
   e=Vx-y=
   \begin{pmatrix}
   0\\
   -1
   \end{pmatrix}.
   $$
   对单样本损失 $\ell(V)=\|Vx-y\|_2^2$，
   $$
   \nabla_V\ell=2ex^T
   =2
   \begin{pmatrix}
   0\\
   -1
   \end{pmatrix}
   \begin{pmatrix}
   2 & 1
   \end{pmatrix}
   =
   \begin{pmatrix}
   0 & 0\\
   -4 & -2
   \end{pmatrix}.
   $$

4. 使用 cross-correlation，不旋转卷积核。
   $$
   y_{11}=1\cdot1+2\cdot(-1)+3\cdot0+1\cdot2=1,
   $$
   $$
   y_{12}=2\cdot1+0\cdot(-1)+1\cdot0+4\cdot2=10,
   $$
   $$
   y_{21}=3\cdot1+1\cdot(-1)+2\cdot0+0\cdot2=2,
   $$
   $$
   y_{22}=1\cdot1+4\cdot(-1)+0\cdot0+1\cdot2=-1.
   $$
   所以输出为
   $$
   \begin{pmatrix}
   1 & 10\\
   2 & -1
   \end{pmatrix}.
   $$

5. 令
   $$
   s_1=Wh_0+Ux_1+b,\quad h_1=\tanh(s_1),
   $$
   $$
   s_2=Wh_1+Ux_2+b,\quad h_2=\tanh(s_2),
   $$
   $$
   e=Y-Z.
   $$
   输出层梯度为
   $$
   \frac{\partial L}{\partial V}=eh_2,\qquad
   \frac{\partial L}{\partial c}=e.
   $$
   设
   $$
   g_2=\frac{\partial L}{\partial s_2}=eV(1-h_2^2),
   $$
   $$
   g_1=\frac{\partial L}{\partial s_1}=g_2W(1-h_1^2).
   $$
   因为 $W,U,b$ 在两个时间步共享，所以
   $$
   \frac{\partial L}{\partial W}=g_2h_1+g_1h_0,
   $$
   $$
   \frac{\partial L}{\partial U}=g_2x_2+g_1x_1,
   $$
   $$
   \frac{\partial L}{\partial b}=g_2+g_1.
   $$

## 三、开放性简答题

1. 残差神经网络的核心是学习
   $$
   y=F(x)+x,
   $$
   其中 $x$ 通过 shortcut connection 直接传到后面层。这样网络不必直接学习完整映射 $H(x)$，而是学习残差 $F(x)=H(x)-x$。当最优映射接近恒等映射时，令 $F(x)\approx0$ 比直接拟合 $H(x)=x$ 更容易。残差连接还为梯度提供了更直接的传播路径，从而缓解深层网络中的梯度消失和退化问题。

2. CNN 与自注意力都可以看成对输入特征进行加权求和。CNN 的权重由卷积核给出，通常是局部的、固定的，并在不同空间位置共享，因此具有平移等变性和较强的局部归纳偏置。自注意力的权重由 $q_i^Tk_j$ 动态计算，依赖输入内容，每个 token 通常可以关注所有 token，因此更擅长全局依赖建模。CNN 更强调局部结构和参数共享，自注意力更强调动态关系和全局交互。

---

# 第二套参考答案

## 一、填空题

1. $a_i-y_i$。

2. $1$；$0$。

3. 过拟合。

4. $\hat{x}_i=\frac{x_i-\mu_B}{\sqrt{\sigma_B^2+\epsilon}}$。

5. $\operatorname{Softmax}\left(\frac{QK^T}{\sqrt{d_k}}\right)V$。

## 二、计算题

1. 对可逆线性变换 $y=Wx$，概率密度满足
   $$
   p_y(y)=p_x(W^{-1}y)|\det W^{-1}|.
   $$
   因此
   $$
   H(y)=-E_{p_y}[\ln p_y(y)].
   $$
   代入密度变换公式可得
   $$
   H(y)=H(x)-\ln|\det W^{-1}|
   =H(x)+\ln|\det W|.
   $$
   若要 $H(y)<H(x)$，需要
   $$
   \ln|\det W|<0,
   $$
   即
   $$
   0<|\det W|<1.
   $$

2. 记 $z_n=w^T\phi(x_n)+b$，$\hat{y}_n=\sigma(z_n)$。交叉熵与 sigmoid 组合有
   $$
   \frac{\partial J}{\partial z_n}=\hat{y}_n-y_n.
   $$
   因此
   $$
   \nabla_w J
   =
   \frac{1}{N}\sum_{n=1}^N(\hat{y}_n-y_n)\phi(x_n)+\lambda w.
   $$
   偏置梯度为
   $$
   \frac{\partial J}{\partial b}
   =
   \frac{1}{N}\sum_{n=1}^N(\hat{y}_n-y_n).
   $$
   最优解满足
   $$
   \frac{1}{N}\sum_{n=1}^N(\hat{y}_n-y_n)\phi(x_n)+\lambda \hat{w}=0,
   $$
   $$
   \frac{1}{N}\sum_{n=1}^N(\hat{y}_n-y_n)=0.
   $$

3. 令
   $$
   z^{(1)}=W^{(1)}x+b^{(1)},\quad
   z^{(2)}=W^{(2)}a^{(1)}+b^{(2)},\quad
   z^{(3)}=W^{(3)}a^{(2)}+b^{(3)}.
   $$
   输出层误差项为
   $$
   \delta^{(3)}=\hat{y}-y.
   $$
   所以
   $$
   \frac{\partial \ell}{\partial W^{(3)}}=\delta^{(3)}(a^{(2)})^T,\qquad
   \frac{\partial \ell}{\partial b^{(3)}}=\delta^{(3)}.
   $$
   第二隐藏层误差为
   $$
   \delta^{(2)}
   =
   (W^{(3)})^T\delta^{(3)}\odot a^{(2)}\odot(1-a^{(2)}),
   $$
   因此
   $$
   \frac{\partial \ell}{\partial W^{(2)}}=\delta^{(2)}(a^{(1)})^T,\qquad
   \frac{\partial \ell}{\partial b^{(2)}}=\delta^{(2)}.
   $$
   第一隐藏层误差为
   $$
   \delta^{(1)}
   =
   (W^{(2)})^T\delta^{(2)}\odot a^{(1)}\odot(1-a^{(1)}),
   $$
   因此
   $$
   \frac{\partial \ell}{\partial W^{(1)}}=\delta^{(1)}x^T,\qquad
   \frac{\partial \ell}{\partial b^{(1)}}=\delta^{(1)}.
   $$
   若所有权重和偏置都初始化为 0，则各层激活为 $0.5$。输出层 $\delta^{(3)}=0.5-y$，所以输出层参数梯度可能非零；但由于 $W^{(3)}=0$，有 $\delta^{(2)}=0$，进一步 $\delta^{(1)}=0$，因此隐藏层梯度为 0。

4. 样本均值为
   $$
   \bar{x}=\frac{1}{3}
   \left[
   \begin{pmatrix}1\\0\end{pmatrix}
   +
   \begin{pmatrix}0\\1\end{pmatrix}
   +
   \begin{pmatrix}1\\1\end{pmatrix}
   \right]
   =
   \begin{pmatrix}
   2/3\\
   2/3
   \end{pmatrix}.
   $$
   中心化样本为
   $$
   \begin{pmatrix}1/3\\-2/3\end{pmatrix},\quad
   \begin{pmatrix}-2/3\\1/3\end{pmatrix},\quad
   \begin{pmatrix}1/3\\1/3\end{pmatrix}.
   $$
   协方差矩阵为
   $$
   S=\frac{1}{3}
   \begin{pmatrix}
   2/3 & -1/3\\
   -1/3 & 2/3
   \end{pmatrix}
   =
   \begin{pmatrix}
   2/9 & -1/9\\
   -1/9 & 2/9
   \end{pmatrix}.
   $$
   该矩阵的最大特征值为 $1/3$，对应特征向量方向为
   $$
   \frac{1}{\sqrt{2}}(1,-1)^T.
   $$
   因此 PCA 第一主成分应选择 $(1,-1)^T$ 方向。

5. 首先
   $$
   QK^T=
   \begin{pmatrix}
   1 & 1\\
   0 & 1
   \end{pmatrix}.
   $$
   按行做 softmax：
   $$
   \operatorname{Softmax}(QK^T)=
   \begin{pmatrix}
   1/2 & 1/2\\
   \frac{1}{1+e} & \frac{e}{1+e}
   \end{pmatrix}.
   $$
   因此
   $$
   \operatorname{Softmax}(QK^T)V
   =
   \begin{pmatrix}
   2 & 1\\
   \frac{1+3e}{1+e} & \frac{2}{1+e}
   \end{pmatrix}.
   $$
   数值上约为
   $$
   \begin{pmatrix}
   2 & 1\\
   2.4621 & 0.5379
   \end{pmatrix}.
   $$

## 三、开放性简答题

1. 过拟合指模型在训练集上误差很低，但在测试集或新样本上误差较高，说明模型过度适应训练样本中的噪声或偶然模式。$L_2$ 正则化通过惩罚过大的权重，使模型更平滑；提前终止在验证误差开始上升时停止训练，避免继续记忆训练集细节；Dropout 在训练时随机丢弃神经元，降低神经元之间的复杂共适应关系，从而提升泛化能力。

2. VAE 包含编码器和解码器。编码器把输入 $x$ 映射为潜变量分布 $q_\phi(z\mid x)$ 的参数，解码器从潜变量 $z$ 重构或生成数据 $p_\theta(x\mid z)$。潜变量用于学习数据的低维隐含表示。重参数化技巧将采样写为 $z=\mu+\sigma\odot\epsilon$，其中 $\epsilon\sim N(0,I)$，从而使梯度可以通过采样过程反向传播。目标函数中的 KL 散度项用于约束后验 $q_\phi(z\mid x)$ 接近先验 $p(z)$，使潜空间连续、规则，并便于从先验采样生成新样本。

---

# 第三套参考答案

## 一、填空题

1. $N(\sqrt{1-\beta_t}x_{t-1},\beta_t I)$。

2. $N(\sqrt{\alpha_t}x_0,(1-\alpha_t)I)$。

3. 真实数据分布；生成样本；真实样本。

4. $D-A$。

5. $m_t$；$v_t$。

## 二、计算题

1. 卷积输出尺寸为
   $$
   W_2=H_2=\frac{32-5+2\times2}{1}+1=32.
   $$
   因此卷积后空间尺寸为 $32\times32$。经过 $2\times2$、步长为 2 的最大池化后，空间尺寸为
   $$
   16\times16.
   $$

2. 将输入按 $2\times2$ 窗口分块：
   $$
   \begin{pmatrix}
   1 & 5\\
   3 & 4
   \end{pmatrix}\to 5,\quad
   \begin{pmatrix}
   2 & 0\\
   6 & 1
   \end{pmatrix}\to 6,
   $$
   $$
   \begin{pmatrix}
   0 & 2\\
   1 & 8
   \end{pmatrix}\to 8,\quad
   \begin{pmatrix}
   7 & 3\\
   2 & 4
   \end{pmatrix}\to 7.
   $$
   输出为
   $$
   \begin{pmatrix}
   5 & 6\\
   8 & 7
   \end{pmatrix}.
   $$

3. 度矩阵为
   $$
   D=
   \begin{pmatrix}
   2 & 0 & 0\\
   0 & 1 & 0\\
   0 & 0 & 1
   \end{pmatrix}.
   $$
   因此
   $$
   L=D-A=
   \begin{pmatrix}
   2 & -1 & -1\\
   -1 & 1 & 0\\
   -1 & 0 & 1
   \end{pmatrix}.
   $$
   计算
   $$
   Lx=
   \begin{pmatrix}
   -4\\
   1\\
   3
   \end{pmatrix}.
   $$
   所以
   $$
   x^TLx=(1,2,4)
   \begin{pmatrix}
   -4\\
   1\\
   3
   \end{pmatrix}
   =-4+2+12=10.
   $$
   另一方面，图中边为 $(1,2)$ 和 $(1,3)$。由于双重求和会把无向边计算两次，
   $$
   \frac{1}{2}\sum_{i,j}A_{ij}(x_i-x_j)^2
   =
   (1-2)^2+(1-4)^2
   =1+9=10.
   $$

4. 因为
   $$
   x_t=\sqrt{1-\beta_t}x_{t-1}+\sqrt{\beta_t}\epsilon_t,
   $$
   且 $x_{t-1}$ 与 $\epsilon_t$ 独立，所以交叉协方差为 0。于是
   $$
   \operatorname{Var}(x_t)
   =
   (1-\beta_t)\operatorname{Var}(x_{t-1})
   +
   \beta_t\operatorname{Var}(\epsilon_t).
   $$
   由题意
   $$
   \operatorname{Var}(x_{t-1})=I,\qquad \operatorname{Var}(\epsilon_t)=I.
   $$
   因此
   $$
   \operatorname{Var}(x_t)
   =
   (1-\beta_t)I+\beta_t I
   =
   I.
   $$

5. 后验满足
   $$
   p(z\mid x)\propto p(x\mid z)p(z).
   $$
   其中
   $$
   p(x\mid z)=N(Wz+\mu,\sigma^2I),\qquad p(z)=N(0,I).
   $$
   对 $z$ 配方可得
   $$
   p(z\mid x)=N(\mu_{z\mid x},\Sigma_{z\mid x}),
   $$
   其中
   $$
   \Sigma_{z\mid x}
   =
   \left(I+\frac{1}{\sigma^2}W^TW\right)^{-1}
   =
   \sigma^2(W^TW+\sigma^2I)^{-1},
   $$
   $$
   \mu_{z\mid x}
   =
   (W^TW+\sigma^2I)^{-1}W^T(x-\mu).
   $$
   一维情形下，$W=2,\mu=1,\sigma^2=1,x=5$，因此
   $$
   \Sigma_{z\mid x}=\frac{1}{2^2+1}=\frac{1}{5},
   $$
   $$
   \mu_{z\mid x}=\frac{1}{2^2+1}\cdot2(5-1)=\frac{8}{5}=1.6.
   $$

## 三、开放性简答题

1. GAN 由生成器和判别器构成，训练目标是一个对抗博弈：生成器希望骗过判别器，判别器希望区分真实样本和生成样本。GAN 采样速度通常较快，因为一次前向传播即可生成样本，但训练可能不稳定，容易出现模式崩塌。扩散模型通过逐步加噪和逐步去噪学习生成过程，训练目标通常是噪声预测或变分下界相关目标，训练更稳定，但采样需要多步迭代，速度较慢。扩散模型通常更容易控制生成质量和多样性，而 GAN 在高效采样方面有优势。

2. RNN 在长序列中需要通过时间反向传播。若循环权重矩阵的有效梯度因子反复相乘，小于 1 的特征值会导致梯度指数级衰减，形成梯度消失；大于 1 的特征值会导致梯度指数级增长，形成梯度爆炸。LSTM 和 GRU 通过门控机制控制信息的写入、遗忘和输出，使隐藏状态中存在更稳定的信息通路，从而缓解长距离依赖学习中的梯度消失问题。对于梯度爆炸，实践中也常配合梯度裁剪。
